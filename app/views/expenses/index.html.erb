<h1>Expenses</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Amount</th>
      <th>Spent date</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @expenses.each do |expense| %>
      <tr>
        <td><%= expense.title %></td>
        <td><%= expense.description %></td>
        <td><%= expense.amount %></td>
        <td><%= expense.spent_date %></td>
        <td><%= link_to 'Show', budget_expense_path(@budget, expense) %></td>
        <td><%= link_to 'Edit', edit_budget_expense_path(@budget, expense) %></td>
        <td><%= link_to 'Destroy', budget_expense_path(@budget, expense), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Expense', new_budget_expense_path(@budget) %>
