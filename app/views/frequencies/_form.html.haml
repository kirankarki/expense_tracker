= form_for(frequency) do |f|
  - if frequency.errors.any?
    #error_explanation
      %h2
        = pluralize(frequency.errors.count, "error")
        prohibited this frequency from being saved:
      %ul
        - frequency.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit
