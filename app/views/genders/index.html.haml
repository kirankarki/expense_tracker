%p#notice= notice
%h1 Genders
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @genders.each do |gender|
      %tr
        %td= gender.name
        %td= link_to 'Show', gender
        %td= link_to 'Edit', edit_gender_path(gender)
        %td= link_to 'Destroy', gender, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Gender', new_gender_path
